# tool_gravity_compensation

For setup, operation, safety notes, and payload dataset validation commands,
see `README_KR240_PAYLOAD_IDENTIFICATION.md`.

Quick dataset validation:

```bash
source /opt/ros/noetic/setup.bash
source devel/setup.bash
catkin_make payload_dataset_validation_report
```
